{
  "name": "ai-project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web系(HTML/CSS/JS)を扱う場合のみ使用。不要なら本ファイルは削除可。",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0"
  }
}
